<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>mz.mohitek.training25xxsample.portal.lab</groupId>
    <artifactId>cedsif-training-environement-base</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    
    <!--License Description-->
    <licenses>
        <license>
            <name>mohitek License, Version 1.0</name>
            <url>https://training.mohitek.net/</url>
            <distribution>repo</distribution>
            <comments>
                This Project module was created and must be used only under academic proposes.
                mail: djo@mohitek.net (business-role) or danilojo@outlook.comn(academic-role)
            </comments>
        </license>
    </licenses>

   <modules>
	<module>cedsif-training-environement-base</module>
	<module>cedsif-training-environement-cli</module>
	<module>cedsif-training-environement-portal</module>
   </modules>

   <dependencyManagement>

   <dependencies>
    <dependency>
      <groupId>com.google.guava</groupId>
      <artifactId>guava</artifactId>
      <version>32.1.1-jre</version>
    </dependency>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.13.1</version>
      <scope>test</scope>
    </dependency>
   </dependencies>
  </dependencyManagement>

   <!-- configuring the max java heap memory can be used by maven execution-->
    <build>
     <plugins>

                <plugin>
                    <!--JavaSE pluggins-->
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.8.0</version>
                    <!--<version>2.3.1</version>-->
                    <configuration>
                        <source>1.8</source>
                        <target>1.8</target>
                    </configuration>
                </plugin>

                 <!-- mvn package to execute-->
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>exec-maven-plugin</artifactId>
                    <version>3.0.0</version>
                    <configuration>
                        <mainClass>${target.class}</mainClass>
                    </configuration>
                </plugin>

                 <plugin>
          <groupId>org.sonarsource.scanner.maven</groupId>
          <artifactId>sonar-maven-plugin</artifactId>
          <version>3.10.0.2594</version>
        </plugin>
        <plugin>
          <groupId>org.jacoco</groupId>
          <artifactId>jacoco-maven-plugin</artifactId>
          <version>0.8.11</version>
        </plugin>

	 <!-- mvn spotbugs:check -->
	<plugin>
	    <groupId>com.github.spotbugs</groupId>
	    <artifactId>spotbugs-maven-plugin</artifactId>
	    <version>4.8.5.0</version>
	    <dependencies>
	        <dependency>
		    <groupId>com.github.spotbugs</groupId>
	            <artifactId>spotbugs</artifactId>
		    <version>4.8.5</version>
	        </dependency>
	    </dependencies>
	</plugin>
	<plugin>
            <groupId>com.github.spotbugs</groupId>
            <artifactId>spotbugs-maven-plugin</artifactId>
            <version>4.9.8.0</version>
            <configuration>
                <includeFilterFile>spotbugs-security-include.xml</includeFilterFile>
                <excludeFilterFile>spotbugs-security-exclude.xml</excludeFilterFile>
                <plugins>
                    <plugin>
                        <groupId>com.h3xstream.findsecbugs</groupId>
                        <artifactId>findsecbugs-plugin</artifactId>
                        <version>1.12.0</version>
                    </plugin>
                </plugins>
            </configuration>
        </plugin>
      </plugins>
     </build>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <target.class>mz.mohitek.training25xxsample.portal.lab.Main</target.class>
    </properties>

    <!--Display Name-->
    <name>cedsif-training-environement</name>

</project>

