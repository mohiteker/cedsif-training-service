
#0.
git config --local user.name "ceXXXXnomeapelido"
git config --local user.email "snake@gmail.com"

#1. fazer o clone do projecto.
git -c http.sslVerify=false \
       clone --branch ceXXXXdakara \
       https://training.dmj-it.net/gitlab/academy-section/a2e2-academy/distributed-system/grupo-x/_23/sample-lab-grupo-x.git


git -c http.sslVerify=false \
       https://training.dmj-it.net/gitlab/academy-section/a2e2-academy/distributed-system/grupo-x/_23/sample-lab-grupo-x.git

#2. copy labs projects do here
cp ... ...
       
#2.
git checkout -b test1-ceXXXXnomeapelido

#3.

#4. criar uma referencia para o repositorio do git
git -c http.sslVerify=false pull origin test1-ceXXXXnomeapelido --force

#5. database configuration

 
git -c http.sslVerify=false pull origin <branch>
--
git add *.png
--
git commit -m "Shifu building test1 and lab-projects"
--
git -c http.sslVerify=false push origin <branch>
--
================================================================
git -c http.sslVerify=false pull origin <branch>
--
git add *.png
--
git commit -m "Shifu building test1 and lab-projects"
--
git -c http.sslVerify=false push origin <branch>
-- 
